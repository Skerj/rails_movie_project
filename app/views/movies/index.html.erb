<h1>Movies</h1>
<ul>
	<% @movies.each do |movie| %>
		<li><%= link_to movie.title, movie_path(movie) %></li>
		Starring: <% movie.actors.each do |actor| %>
		<%= actor.name %>
		<% end %>
	<% end %>
</ul>