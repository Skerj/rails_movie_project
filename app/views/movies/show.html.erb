<h1><%= @movie.title %></h1>
Genre: <%= @movie.genre %><br>
<h4>Starring:</h4>
<% @movie.actors.each do |actor| %>
	<%= link_to actor.name, actor_path(actor) %><br>
<% end %><br>

<!-- have to make a sql statement to select all comments that have movie id of this movie in controller probably(this might not update unless a refresh happens so will probably have to use ajax) -->

<% @movie.comments.each do |comment| %>
	<%= comment.user.username %>: <%= comment.body %><br>
<% end %><br>

<%= link_to "Add comment", new_movie_comment_path(@movie) %>

<!-- here i will have ajax for new comments -->

<br>
<%= link_to "Add actor", '/movies/' + @movie.id.to_s + '/actors/new' %><br>
<%= link_to "Delete movie", movie_path(@movie), method: :delete %><br>
<%= link_to "Show all movies", movies_path %>