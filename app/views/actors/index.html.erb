<h1>Actors</h1>
<ul>
	<%= link_to "Add new actor", new_actor_path %><br>
<!--	
	<%= link_to "Show movies", movies_path %><br>
	<br>
	<%= link_to "View oldest actors", actors_oldest_actors_path %><br>
	<%= link_to "View youngest actors", actors_youngest_actors_path %>
-->
	<% @actors.each do |actor| %>
	<li>
		<h3><%= link_to actor.name, actor_path(actor) %></h3>
		As seen in:<br>
		<% actor.movies.each do |movie| %>
			<%= link_to movie.title, movie_path(movie) %><br>
		<% end %>
	<% end %>
	</li>
</ul>